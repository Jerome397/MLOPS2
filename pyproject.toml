[project]
name = "mlops2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jerome397", email = "jerome.khater@net.usj.edu.lb" }
]
requires-python = ">=3.13"
dependencies = [
    "mlflow>=3.16.1",
    "pillow>=12.3.0",
    "scikit-learn>=1.9.1",
    "torch>=2.14.0",
    "torchvision>=0.29.0",
]

[project.scripts]
mlops2 = "mlops2:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
